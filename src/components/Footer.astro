---
import { SPONSORS } from "./Data";
---

<footer class="bg-black bg-opacity-50 shadow dark:bg-black dark:bg-opacity-50">
  <div class="w-full max-w-full mx-auto p-10 md:py-8 text-center">
    <div class="sm:flex sm:items-center sm:justify-between">
      <a
        href="https://www.instagram.com/dstev.1/"
        class="flex items-center mb-4 sm:mb-0 space-x-3 rtl:space-x-reverse"
      >
        <img
          src="/icons/DSTEV.png"
          class="h-10"
          alt="Flowbite Logo"
        />
      </a>
      <ul class="flex flex-wrap items-center justify-center mb-6 sm:mb-0">
        {
          SPONSORS.map((sponsor) => (
            <li class="mr-4 md:mr-6 transition-transform duration-300 ease-in-out transform hover:-translate-y-1 focus:-translate-y-1">
              <a
                href={sponsor.url}
                class="hover:underline block"
                title={sponsor.nombre}
              >
                <img
                  src={sponsor.logo}
                  alt={`Logo de ${sponsor.nombre}`}
                  class="h-8 mx-auto"
                />
              </a>
            </li>
          ))
        }
      </ul>
    </div>
    <hr class="my-6 border-gray-200 sm:mx-auto dark:border-gray-700 lg:my-8" />
    <span class="block text-sm text-gray-500 sm:text-center dark:text-gray-400"
      >© 2024 - DSTEV Sessions #2 Oficial - Hecho por <a
        href="https://www.instagram.com/santi.salasb/"
        class="hover:underline">Santiago Salas.</a
      ></span
    >
  </div>
</footer>
